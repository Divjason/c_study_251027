#include <stdio.h>

int main(void) // 매개변수, 인수 혹은 인자값
{
	// 어떤 연산처리 이후 값을 저장할 수 있는 공간
	// 변수 variable
	int number1, number2; // 두 변수의 식별자 같다는 전제

	number1 = 1;
	number2 = 2;

	int number3 = 3, number4 = 4; // 두 변수의 식별자 같다는 전제

	printf("%d\n", number1);
	printf("%d\n", number2);

	return 0;
}